/* 
--------------------------------- How Website Works --------------------------------

--> Protocols : Set of rules 

--> HTTP : It stands for HyperText Transfer Protocol.It is a communication protocol
    that is used for communication between client and the server. 
    It determines how data is transfered or shared between client and the server. 

    HTTPS : HTTP + TLS/SSL certificate 
    It is HTTP only but it is more secure.It encrypts the data before sharing so
    that no one can read the data between client and the server. 

    FTP : It stand for File Transfer Protocol.It is used for transfering files. 

    SSH : It stands for secure shell Protocol.It is used to share or transfer files
    over an unsecured network. 

    SMTP : it is used in mails. 

--> How Websites Works ? 
    User send a request to with the help of url.The request (url) goes to the ISP.
    The sends the request to the DNS.The DNS search the IP address of the server
    that is mapped with the requested url.Then DNS returns the IP address of the 
    ISP and ISP returns it to the browser.Browser than directly requests to the 
    server by using it's actual IP address. 

    The browser makes request to the server using it's IP address.The server 
    renders the website on the sever (creates and assembled it on server) and sends
    it back to the client as the response.The browser parse it and renders it as 
    web page. 
    Server generally sends HTML , CSS and JS files.

--> Why we map IP address with the URL ? 
    It is difficult to remember and memorize the IP address of the servers.So the 
    IP address are mapped with the url which are human friendly and easier to 
    remember.

*/
